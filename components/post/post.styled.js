import styled, { css } from 'styled-components';

export const PostPadding = styled.header`
  padding: var(--spacing2);
`;

export const PostContainer = styled.header`
  padding: var(--spacing4) var(--spacing2);
`;
